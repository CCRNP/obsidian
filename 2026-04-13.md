### 重建Docker镜像，满足ModelArts规范
**启动该容器并进行修改**
docker run -it --name temp_container ${IMAGES:ID} /bin/bash

容器内部 创建 ma-user 用户，ModelArts要求容器内必须有特定的用户
groupadd -g 100 ma-group
useradd -u 1000 -g 100 -m ma-user

退出容器后，使用 `docker commit` 命令，将刚才对容器的所有修改保存为一个全新的镜像
docker commit temp_container ${IMAGES:ID}}