### AK/SK 获取 Token 完整总结

#### 一、为什么用 AK/SK？

当用户名/密码获取 Token 遇到 401（尤其是跨 VDC、权限配置复杂时），AK/SK 是更可靠的替代方案：

| 对比 | 用户名/密码 | AK/SK |
|---|---|---|
| 认证方式 | 账号密码直连 | 密钥对签名认证 |
| 跨 VDC 兼容性 | 容易因 VDC 层级/作用域报错 | 不受 VDC 层级影响，只认 AK/SK |
| 安全性 | 密码可能泄露 | 密钥可随时禁用/轮换 |
| 推荐场景 | 简单测试 | **生产环境、跨租户调用** |


#### 二、操作流程（四步）

**第 1 步：生成 AK/SK**

让目标用户（如 `dengqiuzhi`）登录 ManageOne 运营面：
- 右上角头像 → **个人设置**
- 点击 **管理访问密钥** 页签
- 点击 **新增访问密钥**
- 系统自动下载 `credentials.csv`，内含：
  - **AK（Access Key ID）**：如 `HPUAMKL83ONQEYVOVSCV`
  - **SK（Secret Access Key）**：对应的密钥

> ⚠️ 每个用户最多 2 个访问密钥，AK/SK 仅生成时下载一次，务必保存。

**第 2 步：准备参数**

| 参数 | 获取位置 |
|---|---|
| AK / SK | 上一步的 credentials.csv |
| Project ID | 个人设置 → 资源空间列表 |
| Domain Name | 个人设置 → 租户名（如 `JSZN`） |
| IAM 用户名 | 如 `dengqiuzhi` |
| 密码 | 该用户登录密码 |

**第 3 步：安装依赖**

Mac 上 pip 用不了，用 `pip3`：

```bash
pip3 install requests
```

下载华为云 Python 签名 SDK（`ApiGateway-python-sdk`），解压后得到 `apig_sdk` 文件夹，放在脚本同目录下。

**第 4 步：编写并运行脚本**

核心逻辑：**用 SDK 的 `HttpRequest` 构造请求 → 签名 → 用 requests 发送**。

```python
import json, requests, sys, os
sys.path.append(os.path.join(os.path.dirname(os.path.abspath(__file__)), 'apig_sdk'))
from apig_sdk import signer

AK = "你的AK"
SK = "你的SK"
PROJECT_ID = "你的Project ID"
IAM_ENDPOINT = "https://iam-apigateway-proxy.gdrising-global-1.air.gdrising.com.cn"
DOMAIN_NAME = "JSZN"
USERNAME = "dengqiuzhi"
PASSWORD = "密码"

payload = {
    "auth": {
        "identity": {
            "methods": ["password"],
            "password": {
                "user": {
                    "name": USERNAME,
                    "password": PASSWORD,
                    "domain": {"name": DOMAIN_NAME}
                }
            }
        },
        "scope": {"project": {"id": PROJECT_ID}}
    }
}

url = f"{IAM_ENDPOINT}/v3/auth/tokens"

# 关键：用SDK自带的HttpRequest，不能用requests.Request
req = signer.HttpRequest("POST", url)
req.headers = {"Content-Type": "application/json"}
req.body = json.dumps(payload)

sig = signer.Signer()
sig.Key = AK
sig.Secret = SK
sig.Sign(req)

# 发送请求（注意用原始url变量，HttpRequest没有.url属性）
resp = requests.post(url, headers=req.headers, data=req.body,
                     timeout=30, verify=False)

print("状态码:", resp.status_code)
print("Token:", resp.headers.get("X-Subject-Token"))
print("响应内容:", resp.text)
```

运行：
```bash
python3 getToken.py
```

成功时状态码为 `201`，`X-Subject-Token` 就是你要的 Token。


#### 三、踩过的坑（重要经验）

| 报错 | 原因 | 解决 |
|---|---|---|
| `'Request' object has no attribute 'body'` | 用了 `requests.Request`，SDK 只认 `signer.HttpRequest` | 改用 `signer.HttpRequest("POST", url)` 构造 |
| `'HttpRequest' object has no attribute 'url'` | `HttpRequest` 没有 `.url` 属性 | 发送时用原始的 `url` 变量，不用 `req.url` |
| `pip` 命令找不到 | Mac 默认只有 `python3`/`pip3` | 用 `pip3` 或 `python3 -m pip` |
| `NotOpenSSLWarning` | Mac 自带 LibreSSL，urllib3 v2 要求 OpenSSL 1.1.1+ | 只是警告，不影响功能，可忽略 |
| SSL 证书校验失败 | 私有云自签证书 | 请求时加 `verify=False` |


#### 四、关键要点

1. **签名对象类型要对**：SDK 的 `Sign()` 方法接收的是 `signer.HttpRequest`，不是 `requests.Request`。
2. **发送时用原始 url**：`HttpRequest` 只有 `headers` 和 `body` 属性，URL 单独用变量传。
3. **`verify=False`**：私有云环境证书通常自签，测试时需跳过 SSL 校验。
4. **Token 有效期 24 小时**：建议应用侧缓存，避免频繁获取。
5. **AK/SK 比密码更安全**：可随时禁用、轮换，不受 VDC 层级影响，适合生产环境。


#### 五、一句话总结

> 用 AK/SK 获取 Token 的核心是：**生成密钥 → 用 SDK 的 `HttpRequest` 构造请求 → 签名 → 用 `requests` 发送**。相比用户名/密码方式，它绕开了 VDC 权限层级和 401 报错的坑，是跨租户、生产环境调用的推荐方式。