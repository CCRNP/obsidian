
“输入是什么、输出是什么、怎么算分”

输入：“标准答案 + 模型回答”
测评脚本的输入 = 评测数据集里的真实标签（y_true）+ 调用模型API后得到的预测结果（y_pred）。
数管平台负责这两列数据凑齐，脚本只负责拿它们算分，先把评测数据集准备好，确保格式正确，平台后端能逐条读取、调用API、收集结果，输入这一步就完成。

输出：输出是“算好的分数+错在哪”
测评脚本跑完，返回的就是一个 Python 字典（dict），里面装着算好的各项指标。
根据指标生成测试报告。或者这个字典会被数据管理平台接收，然后存进数据库、展示在前端。

算分：测评脚本中的算分逻辑
拿到两列数据（标准答案和模型回答），根据场景选公式，输出指标字典。

平台后端流程：数管平台——调度中心

前端（数据管理平台页面）
    ↓ 创建评测任务、查看结果
后端调度层（数据管理平台）
    ↓ 调度、调用、存储
执行层（训推平台在线服务 + 测评脚本）
    ↓ 返回推理结果、指标
存储层（数据库 + 文件存储）



脚本样例
```python
# eval_metrics.py
import numpy as np
from sklearn.metrics import accuracy_score, precision_recall_fscore_support, confusion_matrix

def compute_classification_metrics(y_true, y_pred, labels=None):
    """
    通用分类任务测评指标
    :param y_true: list, 真实标签
    :param y_pred: list, 预测标签
    :param labels: list, 类别名称（可选，用于混淆矩阵和分类报告）
    :return: dict, 指标字典
    """
    accuracy = accuracy_score(y_true, y_pred)
    precision, recall, f1, _ = precision_recall_fscore_support(
        y_true, y_pred, average='macro', zero_division=0
    )
    cnf_matrix = confusion_matrix(y_true, y_pred, labels=labels).tolist()

    # 逐类别指标
    per_class = {}
    if labels:
        p, r, f, s = precision_recall_fscore_support(
            y_true, y_pred, labels=labels, zero_division=0
        )
        for i, label in enumerate(labels):
            per_class[label] = {
                "precision": round(float(p[i]), 4),
                "recall": round(float(r[i]), 4),
                "f1": round(float(f[i]), 4),
                "support": int(s[i])
            }

    return {
        "accuracy": round(float(accuracy), 4),
        "precision": round(float(precision), 4),
        "recall": round(float(recall), 4),
        "f1": round(float(f1), 4),
        "confusion_matrix": cnf_matrix,
        "per_class": per_class
    }


def compute_face_metrics(y_true, similarity_scores, thresholds=None):
    """
    人脸识别测评指标（FAR/FRR/EER/TAR@FAR）
    :param y_true: list, 1表示同一人，0表示不同人
    :param similarity_scores: list, 模型输出的相似度分数（0~1）
    :param thresholds: list, 遍历的阈值，默认0.1~0.95
    :return: dict, 指标字典
    """
    if thresholds is None:
        thresholds = np.arange(0.1, 0.96, 0.01)

    results = []
    for thresh in thresholds:
        preds = [1 if s >= thresh else 0 for s in similarity_scores]
        TP = sum(1 for t, p in zip(y_true, preds) if t == 1 and p == 1)
        TN = sum(1 for t, p in zip(y_true, preds) if t == 0 and p == 0)
        FP = sum(1 for t, p in zip(y_true, preds) if t == 0 and p == 1)
        FN = sum(1 for t, p in zip(y_true, preds) if t == 1 and p == 0)

        FAR = FP / (FP + TN) if (FP + TN) > 0 else 0.0
        FRR = FN / (FN + TP) if (FN + TP) > 0 else 0.0
        results.append({
            "threshold": round(float(thresh), 2),
            "FAR": round(float(FAR), 6),
            "FRR": round(float(FRR), 6)
        })

    # 找EER（FAR与FRR最接近的点）
    eer_point = min(results, key=lambda x: abs(x["FAR"] - x["FRR"]))
    eer = eer_point["FAR"]
    eer_threshold = eer_point["threshold"]

    # 计算TAR@FAR（如FAR=1e-4, 1e-3）
    tar_at_far = {}
    for target_far in [1e-4, 1e-3]:
        for r in results:
            if r["FAR"] <= target_far:
                tar_at_far[f"TAR@FAR={target_far}"] = round(1 - r["FRR"], 4)
                break
        else:
            tar_at_far[f"TAR@FAR={target_far}"] = None

    return {
        "EER": round(float(eer), 4),
        "EER_threshold": eer_threshold,
        "TAR@FAR": tar_at_far,
        "threshold_sweep": results
    }


# 以下是本地测试代码，平台调用时不需要执行
if __name__ == "__main__":
    # 分类示例
    y_true_cls = ["猫", "狗", "猫", "猫", "狗", "狗", "猫", "狗"]
    y_pred_cls = ["猫", "狗", "狗", "猫", "狗", "猫", "猫", "狗"]
    labels = ["猫", "狗"]
    cls_metrics = compute_classification_metrics(y_true_cls, y_pred_cls, labels)
    print("分类指标：")
    for k, v in cls_metrics.items():
        if k not in ("confusion_matrix", "per_class"):
            print(f"  {k}: {v}")
    print("  混淆矩阵:", cls_metrics["confusion_matrix"])

    # 人脸识别示例（模拟100对样本）
    np.random.seed(42)
    y_true_face = [1] * 50 + [0] * 50
    # 同一人相似度偏高，不同人相似度偏低
    scores_same = np.random.normal(0.75, 0.1, 50).clip(0, 1)
    scores_diff = np.random.normal(0.25, 0.1, 50).clip(0, 1)
    similarity_scores = list(scores_same) + list(scores_diff)
    face_metrics = compute_face_metrics(y_true_face, similarity_scores)
    print("\n人脸识别指标：")
    print(f"  EER: {face_metrics['EER']}")
    print(f"  EER阈值: {face_metrics['EER_threshold']}")
    print(f"  TAR@FAR: {face_metrics['TAR@FAR']}")
```


## 输出给前端展示什么？

数据管理平台前端一般展示这几块：

|展示模块|内容|数据来源|
|---|---|---|
|**指标卡片**|准确率、F1、EER等核心指标|汇总指标|
|**混淆矩阵热力图**|哪类容易混淆|`confusion_matrix`|
|**阈值曲线图**|FAR/FRR随阈值变化的曲线|`threshold_sweep`|
|**逐条明细表**|每条数据的输入、预测、真实标签|逐条明细|
|**错误样本筛选**|只看预测错的|逐条明细|
|**对比报告**|多个模型/版本的指标对比|多次评测结果|


不用一上来做全，可按这个顺序跑通：

|步骤|做什么|验证方式|
|---|---|---|
|1|写一个最简单的测评脚本 `compute_metrics`|本地用假数据跑通|
|2|后端实现“读JSONL + 调推理API + 调脚本”|用10条数据跑通|
|3|加异步任务和状态更新|前端能查到进度|
|4|加结果存储和前端展示|能看到指标和明细|
|5|加并发、重试、断点续跑|压测1000条数据|

**平台后端调度的核心就是一条流水线：**

> 创建任务 → 异步执行 → 逐条调推理API → 收集预测结果 → 调测评脚本算分 → 存结果 → 更新状态 → 前端展示。